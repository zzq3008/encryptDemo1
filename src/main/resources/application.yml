##thymeleaf页面模板配置
spring:
  mvc:
    view:
      prefix: /
      suffix: .html

      
  activemq:
    user: admin
    password: admin
    broker-url: tcp://127.0.0.1:61616
    pool:
      enabled: true
      max-connections: 10

queueName: publish.queue
topicName: publish.topic